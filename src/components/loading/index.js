import React from 'react';
import { Container, Headline, Wrapper } from './Loading.styles';

const Loading = () => {
  return (
    <Container>
      <Wrapper>
        <Headline>Loading...</Headline>
      </Wrapper>
    </Container>
  );
};

export default Loading;
